# 论文精读 飞书推送 写作提示词

> 用途：当你需要为一篇学术论文写飞书推送版本时，使用以下提示词。
> 目标受众：有推荐系统/搜广推基础但对论文不熟悉的学生。
> 风格定位：**learn-note 紧凑风格 + 飞书消息字数限制**。
> 字数控制：全文 1500-2500 字（中文），确保飞书消息不截断。

---

## 一、文档头部

格式：

```markdown
**{中文标题：论文核心思想一句话概括}**

{作者名} | {年份-月-日}
🔗 {原文链接}

---

## 30 秒类比

{一个贴近日常的类比，让读者 30 秒理解论文在做什么}

---
```

---

## 二、正文结构（按顺序，严格控制字数）

### 1. 要解决什么问题

**必须写。** 用 2-3 段把问题讲清楚。

写作要点：
- 第一段：**这个场景现在是怎么做的**（传统做法）
- 第二段：**传统做法有什么缺陷**（为什么解决不了）
- 第三段（可选）：**如果问题不解决会怎样**（量化损失）

> 每段不超过 5 行。12 号字在飞书上一段超过 8 行就没人读了。

---

### 2. 已有方法对比

**强烈推荐。** 用表格展示论文方法和现有方法的差异。

格式：

```
| 方法 | 做法 | 缺点 |
|------|------|------|
| 方法 A | 一句话 | 一句话 |
| 方法 B | 一句话 | 一句话 |
| **本论文** | 一句话 | — |
```

> 表格不超过 5 行。表格后跟 1-2 段展开说明核心差异。

---

### 3. 核心方法拆解

**这是正文最核心的部分。** 拆成 2-4 个模块，每个模块的写作格式：

```markdown
### 模块名：一句话说明

**做了什么**：这个模块输入是什么、输出是什么、怎么做的。

{具体说明，2-4 段}

{如果有公式：用纯文本 + 数值示例，禁止 LaTeX}
{如果有伪代码：用 ```text 包裹，5-10 行，每行中文注释}
```

写作要点：
- **先讲概念，再放伪代码/公式。** 不要让读者在不懂概念的情况下看代码
- **每个模块必须有输入/输出说明。** 读者要知道这个模块吃了什么、吐出什么
- **公式后必须跟数值示例。** 纯公式看不懂，套数字就算看懂了
- **伪代码每行中文注释。** 说"这段在干什么"，不说"这行是什么语法"
- **模块数量控制：2-4 个。** 超过 4 个读者记不住

---

### 4. 实验结果 + 业务含义

**必须写。** 用表格展示关键指标。

格式：

```markdown
| 场景 | 论文方法 | 基线 | 提升 |
|------|---------|------|------|
| 场景 A | XX% | YY% | +ZZ% |
| 场景 B | XX% | YY% | +ZZ% |

**业务含义**：每 100 次需求，论文方法比基线多完成 ZZ 次。这意味着 {具体业务收益}。
```

---

### 5. 局限性

**必须写。** 列出 3-5 个论文没提但实践中会遇到的问题。

格式：每行一个，用 `| 问题 | 说明 |` 紧凑表格或简洁列表。

---

### 6. 面试一句话

**必须写。** 一句话总结论文核心贡献，50 字以内，能直接用在面试回答里。

---

## 三、写作风格规范

### 语言风格
1. **和学生对话**：语气平实、直接。用"你"而不是"读者"
2. **紧凑**：不需要的修饰词全删掉。"非常"、"极其"、"显著地" → 删
4. **不说项目结合**：这是飞书推送，不分析读者项目。纯论文精读

### 格式规范
1. **表格优先**：数据对比、方法对比、实验效果，能用表格就不要写段落
2. **代码块用 ` ```text `**：不要用 ` ```python `（飞书消息里代码块渲染不稳定）
3. **不要 LaTeX**：公式用纯文本 + 数值示例。禁止 `$`、`$$`、`\frac`
4. **不要 Mermaid**：飞书消息不支持 Mermaid 渲染
5. **善用粗体和分隔线**：`---` 分隔不同大段，粗体突出关键结论
6. **不要 `> 引用` 块**：飞书消息中引用块字数也算在总限制里，且阅读体验不好

### 篇幅控制（最重要）

```
总字数：1500-2500 字（中文）
各部分占比：
  头部（标题+链接+类比）：15%
  问题 + 方法对比：25%
  核心方法（2-4 个模块）：35%
  实验 + 局限 + 面试话：25%
```

> ⚠️ 写完后统计一次中文字数。超过 2500 字必须删减。  
> ⚠️ 每条消息都可能被飞书截断。最关键的"核心方法拆解"部分一定不要被截断。

### 检查清单

- [ ] 总字数 1500-2500 字
- [ ] 去掉了"能做的 vs 不能做的"章节
- [ ] 去掉了"对项目的思考"章节
- [ ] 有 30 秒类比
- [ ] 方法对比表格不超过 5 行
- [ ] 核心方法 2-4 个模块，每个有输入/输出
- [ ] 公式用纯文本 + 数值示例，无 LaTeX
- [ ] 实验有表格 + 业务含义
- [ ] 有局限性列表
- [ ] 面试一句话 ≤ 50 字
- [ ] 没有 Mermaid、没有引用块、没有 `$` 公式
